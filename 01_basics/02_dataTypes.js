"use strict"; //  use all JS code as newer version //

// alert(3 + 3) // we are using nodejs, not browser so it give error.//

console.log("sam");

let name = "samarth"
let age = 22
let isLoggedIn = false

/* number => 2 power 53
   bigint = 
   string => ""
   boolean => true/false
   null => standalone value (type)
   undefined => value not defined like (let state;)
   symbols => unique

   object

   */

   console.log(typeof undefined); // o/p: undefined //
   console.log(typeof null); // o/p: object //
   
   
